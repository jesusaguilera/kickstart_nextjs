// Dependencies
import React from "react";
import Head from "next/head";

const Header = (props) => {
  return (
    <header className="c-header">
      <h3>header</h3>
    </header>
  );
};
export default Header;
